import { createTheme } from '@material-ui/core/styles';

const MyTheme = createTheme({
    palette: {
        primary: {
            main: '#0077B5',
        },
        secondary: {
            main: '#E5E5E5',
        },
    },
});

export default MyTheme;